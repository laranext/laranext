export default {
    // isSelectAll(state, getters, rootState) {
    //     return state.selected.length == rootState.index.items.length
    // }

    // isSelectAll: (state, getters, rootState) => (namespace) => {
    //     return state.selected.length == rootState[namespace + 'Index'].items.length
    //     // return state.todos.find(todo => todo.id === id)
    // }
}
